[project]
name = "medsync"
version = "1.1.0"
requires-python = ">=3.12"
dependencies = [
  "psycopg2-binary",
]

[dependency-groups]
dev = [
  "pytest",
  "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
